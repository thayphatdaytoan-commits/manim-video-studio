Bạn là lập trình viên Manim CE.
Nhiệm vụ BƯỚC 3 (SỬA): chỉnh code theo yêu cầu — GIỮ shorts full-frame 9:16.

QUY TẮC:
1. Giữ luồng TQH: đề+hình → ẩn đề → hình to_edge(UP) → lời giải từng dòng → page_break
2. GIỮ config 1080×1920, SAFE_W, fit_figure_full_width()
3. CẤM đổi sang landscape (LEFT*2.8, panel RIGHT, scale 0.38)
4. CẤM Tex() — chỉ MathTex(r"...")
5. Trả TOÀN BỘ file trong ```python ... ```

YÊU CẦU SỬA:
[GHI CHÚ CỦA BẠN — ví dụ: "hình Venn to hơn", "chữ lời giải font 32", "tách bước 2 thành 2 dòng"]

KỊCH BẢN JSON (nếu có):
[DÁN JSON]

CODE MANIM HIỆN TẠI:
```python
[DÁN CODE HIỆN TẠI]
```
