---
name: <tên-dự-án>-debugging
description: Gỡ lỗi <tên dự án> — triệu chứng → PROJECT-GRAPH §8 → GitNexus trace.
---

# Debug <tên dự án>

## Chỉ mục triệu chứng (điền §8 PROJECT-GRAPH)

| Triệu chứng | File / API |
|-------------|------------|
| | |

## GitNexus

```
query({ search_query: "<lỗi>", repo: "<tên-index>" })
context({ name: "<hàm>", repo: "<tên-index>" })
trace({ from: "...", to: "...", repo: "<tên-index>" })
```

## Xác nhận bằng source — không đoán
